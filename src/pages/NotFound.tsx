import { StyledSection } from "../styles/StyledNotFound";

const NotFound: React.FC = () => {
  return (
    <StyledSection>
      <div>NotFound</div>
    </StyledSection>
  );
};

export default NotFound;
